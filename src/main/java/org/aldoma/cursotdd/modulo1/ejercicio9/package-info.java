/**
 * Noveno ejercicio del módulo 1.
 * <p>
 * <h2>Enunciado del ejercicio:</h2>
 * <p>
 * Contamos con una clase {@code C} con un atributo {@code a} (por ejemplo de tipo
 * {@link java.lang.String}) y un método {@code getA} que nos devuelve su valor. También disponemos
 * de una factoría {@code F} con un método {@code creaLista} que no admite ningún parámetro y crea y
 * devuelve una lista (interfaz {@link List}) de objetos de clase {@code C} en el que cada objeto
 * tiene un valor en su atributo a distinto de los demás.
 * <p>
 * Escriba utilizando una herramienta XUnit un caso de prueba que verifique que todos los objetos en
 * la lista devuelta por el método {@code creaLista} son distintos. Intente no presuponer el orden
 * de los elementos ni valores concretos para el atributo {@code a}.
 * 
 * @author Alberto Dominguez Matamoros
 */
package org.aldoma.cursotdd.modulo1.ejercicio9;

